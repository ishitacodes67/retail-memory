# retail-memory
![CI](https://github.com/ishitacodes67/retail-memory/actions/workflows/ci.yml/badge.svg)

An LLM tool-calling agent that tells you when a retail discount is working, and when it's quietly eating your own sales.

## The problem

TODO: 4-6 sentences in your own words. A shop discounts a product by 20% and sales rise. Why isn't that enough to call the discount a success?

## What it will do

- `get_sales_summary(product, period)` — TODO: what it takes in and returns
- `recommend_markdown(product, margin)` — TODO: what it takes in and returns
- `detect_cannibalization(promo_product, neighbor)` — TODO: what it takes in and returns

## Status

In progress — Day 1 of build.